spring.application.name=ApiGatewayServices
server.port=8769

# Remove explicit routes, use only discovery locator
spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

# No explicit routes - let gateway auto-create them